import React, { Component } from "react";

import dbList from "../data/stm32_gm2.json";

export class DeviceList extends Component {
  render() {
    // const localDBList = JSON.parse(dbList);
    console.log(dbList);
    // console.log(localDBList);
    return <div></div>;
  }
}

export default DeviceList;
